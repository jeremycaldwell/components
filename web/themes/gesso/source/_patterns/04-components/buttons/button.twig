{% if button %}
  <button class="button {{ modifier }}"{% if disabled %} disabled{% endif %}>{{ text }}</button>
{% else %}
  <a href="{{ url }}" target="{{ target|default('_blank') }}" class="button {{ modifier }}">{{ text }}</a>
{% endif %}
