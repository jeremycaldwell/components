<div class="l-{{ region_name ?: 'region' }}">
  {% if constrain %}
    <div class="{{ constrain }}">
  {% endif %}
  {% block content %}
    Region Content
  {% endblock %}
  {% if constrain %}
    </div>
  {% endif %}
</div>
